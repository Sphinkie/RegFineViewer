<Window x:Class="RegFineViewer.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Registry Fine Viewer" Height="640"  MaxHeight="920"  Width="860"   MinWidth="860"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        TextElement.FontWeight="Regular"
        TextElement.FontSize="13"
        TextOptions.TextFormattingMode="Ideal"
        TextOptions.TextRenderingMode="Auto"
        Background="{DynamicResource MaterialDesignPaper}"
        FontFamily="{DynamicResource MaterialDesignFont}"
        xmlns:local="clr-namespace:RegFineViewer"
        Name="Fenetre"
        >

  <!--On définit quelques styles généraux-->
  <Window.Resources>
    <Style TargetType="materialDesign:Chip" >
      <Setter Property="Margin" Value="16,0,0,0"/>
    </Style>

    <Style TargetType="Button" x:Key="OutlinedSmallButton"  BasedOn="{StaticResource MaterialDesignOutlinedButton}" >
      <Setter Property="Width"      Value="60"/>
      <Setter Property="Height"     Value="20" />
      <Setter Property="Padding"    Value="0"/>
      <Setter Property="FontSize"   Value="12" />
      <Setter Property="FontWeight" Value="Light" />
    </Style>

    <!--<SolidColorBrush x:Key="{x:Static SystemColors.HighlightBrushKey}" Color="Green" />-->

    <!--<Style TargetType="TreeViewItem" >
            <Setter Property="IsSelected" Value="{Binding IsSelected}" />
            <Setter Property="IsExpanded" Value="{Binding IsExpanded}" />
        </Style>-->

<<<<<<< Updated upstream
    </Window.Resources>

    <DockPanel Name="dockpanel">
        <!-- On definit une grille de 2 colonnes: Left et Right -->
        <Grid >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" Name="leftColumn" MinWidth="420"/>
                <ColumnDefinition Width="5" Name="separatorColumn"/>
                <ColumnDefinition Width="*" Name="rightColumn" MinWidth="420"/>
            </Grid.ColumnDefinitions>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" Name="ToolbarRow"  />
                <RowDefinition Height="Auto" Name="TitleRow"    />
                <RowDefinition Height="*"    Name="TreeViewRow" />
                <RowDefinition Height="Auto" Name="EditionRow"  />
                <RowDefinition Height="Auto" Name="SearchbarRow" />
            </Grid.RowDefinitions>

            <!-- *************************************************************** -->
            <!--                     Grille de Gauche                            -->
            <!-- *************************************************************** -->
            <!-- ==========================-->
            <!-- Toolbar 1 -->
            <!-- ==========================-->
            <ToolBarTray DockPanel.Dock="top" Grid.Column="0" Grid.Row="0">
                <ToolBar Style="{DynamicResource MaterialDesignToolBar}" >
                    <Button ToolTip="Create a Test tree" Click="FillRegistryTree">
                        <materialDesign:PackIcon Kind="ExpandLess"/>
                    </Button>
                    <Button ToolTip="Tree stats" Name="bTray1Button1" Click="bTray1Button1_Click">
                        <materialDesign:PackIcon Kind="ChartBarStacked" />
                    </Button>
                    <Button ToolTip="Length stats" Name="bTray1Button2" Click="bTray1Button2_Click">
                        <materialDesign:PackIcon Kind="ChartGantt" />
                    </Button>
                </ToolBar>
            </ToolBarTray>

            <!-- ==========================-->
            <!-- CHIP 1 -->
            <!-- ==========================-->
            <materialDesign:Chip Grid.Column="0" Grid.Row="1" x:Name="Tree1_InfoChip"
          Content="no file loaded"
          IsDeletable="True" DeleteClick="Tree1_CloseFile_bt"  DeleteToolTip="Close the file">
                <materialDesign:Chip.Icon>
                    <materialDesign:PackIcon  Kind="File"></materialDesign:PackIcon>
                </materialDesign:Chip.Icon>
            </materialDesign:Chip>

            <!-- ==========================-->
            <!-- DROP ZONE 1  -->
            <!-- ==========================-->
            <TextBlock x:Name="DropZone1" Grid.Column="0" Grid.Row="2" AllowDrop="True" Drop="Tree1_drop" 
                       Text="drop your registry file here" 
                       FontSize="48" Foreground="LightGray" TextAlignment="Center" TextWrapping="Wrap" VerticalAlignment="Center" Margin="40,0,40,0" Focusable="False" />
            <!-- ==========================-->
            <!-- TREE VIEW 1  -->
            <!-- ==========================-->
            <TreeView Name="TreeView1" Grid.Column="0" Grid.Row="2" Margin="16,16,0,0" Visibility="Hidden"
                AllowDrop="True" Drop="Tree1_drop"
                      VirtualizingStackPanel.IsVirtualizing="True"
                      VirtualizingStackPanel.VirtualizationMode ="Recycling"
                SelectedItemChanged="TreeView1_SelectedItemChanged">

                <TreeView.Resources>
                    <HierarchicalDataTemplate
                    DataType="{x:Type local:RegistryItem}"
                    ItemsSource="{Binding Path=SubItem}">

                        <TreeViewItem 
                            Selected="TreeViewItem_OnItemSelected"
                            IsSelected="{Binding IsSelected, Mode=TwoWay}"
                            IsExpanded="{Binding IsExpanded, Mode=TwoWay}"
                            local:BringSelectedItemIntoViewBehavior.IsBringSelectedIntoView="True" 
=======
  </Window.Resources>

  <DockPanel Name="dockpanel">
    <!-- On definit une grille de 2 colonnes: Left et Right -->
    <Grid >
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="*" Name="leftColumn" MinWidth="420"/>
        <ColumnDefinition Width="5" Name="separatorColumn"/>
        <ColumnDefinition Width="0" Name="rightColumn" />
        <!-- MinWidth="420" Width="*"/-->
      </Grid.ColumnDefinitions>
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto" Name="ToolbarRow"  />
        <RowDefinition Height="Auto" Name="TitleRow"    />
        <RowDefinition Height="*"    Name="TreeViewRow" />
        <RowDefinition Height="Auto" Name="EditionRow"  />
        <RowDefinition Height="Auto" Name="SearchbarRow" />
      </Grid.RowDefinitions>

      <!-- *************************************************************** -->
      <!--                     Grille de Gauche                            -->
      <!-- *************************************************************** -->
      <!-- ==========================-->
      <!-- Toolbar 1 -->
      <!-- ==========================-->
      <ToolBarTray DockPanel.Dock="top" Grid.Column="0" Grid.Row="0">
        <ToolBar Style="{DynamicResource MaterialDesignToolBar}" >
          <Button ToolTip="Create a Test tree" Click="FillRegistryTree">
            <materialDesign:PackIcon Kind="ExpandLess"/>
          </Button>
          <Button ToolTip="Tree stats" Name="bTray1Button1" Click="bTray1Button1_Click">
            <materialDesign:PackIcon Kind="ChartBarStacked" />
          </Button>
          <Button ToolTip="Length stats" Name="bTray1Button2" Click="bTray1Button2_Click">
            <materialDesign:PackIcon Kind="ChartGantt" />
          </Button>
        </ToolBar>
      </ToolBarTray>

      <!-- ==========================-->
      <!-- CHIP 1 -->
      <!-- ==========================-->
      <materialDesign:Chip Grid.Column="0" Grid.Row="1" x:Name="Tree1_InfoChip"
    Content="no file loaded"
    IsDeletable="True" DeleteClick="Tree1_CloseFile_bt"  DeleteToolTip="Close the file">
        <materialDesign:Chip.Icon>
          <materialDesign:PackIcon  Kind="File"></materialDesign:PackIcon>
        </materialDesign:Chip.Icon>
      </materialDesign:Chip>

      <!-- ==========================-->
      <!-- DROP ZONE 1  -->
      <!-- ==========================-->
      <TextBlock x:Name="DropZone1" Grid.Column="0" Grid.Row="2" AllowDrop="True" Drop="Tree1_drop"
                 Text="drop your registry file here"
                 FontSize="48" Foreground="LightGray" TextAlignment="Center" TextWrapping="Wrap" VerticalAlignment="Center" Margin="40,0,40,0" Focusable="False" />
      <!-- ==========================-->
      <!-- TREE VIEW 1  -->
      <!-- ==========================-->
      <TreeView Name="TreeView1" Grid.Column="0" Grid.Row="2" Margin="16,16,0,0" Visibility="Hidden"
          AllowDrop="True" Drop="Tree1_drop"
                VirtualizingStackPanel.IsVirtualizing="True"
                VirtualizingStackPanel.VirtualizationMode ="Recycling"
          SelectedItemChanged="TreeView1_SelectedItemChanged">

        <TreeView.Resources>
          <HierarchicalDataTemplate
          DataType="{x:Type local:RegistryItem}"
          ItemsSource="{Binding Path=SubItem}">

            <TreeViewItem
                Selected="TreeViewItem_OnItemSelected"
                IsSelected="{Binding IsSelected, Mode=TwoWay}"
                IsExpanded="{Binding IsExpanded, Mode=TwoWay}"
                local:BringSelectedItemIntoViewBehavior.IsBringSelectedIntoView="True"
>>>>>>> Stashed changes
                            >
              <TreeViewItem.Header>
                <!-- Affichage par défaut des données -->
                <StackPanel Orientation="Horizontal" MinHeight="0"  Margin="0,-6,0,-6" >
                  <TextBlock x:Name="lbName"    Text="{Binding Path=Name}"  Width="220" ToolTip="{Binding Path=Name}" ToolTipService.InitialShowDelay="2000"/>
                  <TextBlock x:Name="lbValue"   Text="{Binding Path=Value}" ToolTip="{Binding Path=DType}" Width="80" />
                  <Button    x:Name="btUfUnit"  Content="{Binding Path=UserFriendlyUnit}"  Click="TreeView_ChangeUnit_Click"  Style="{StaticResource OutlinedSmallButton}" Visibility="Hidden" />
                  <TextBlock x:Name="lbUfValue" Text="{Binding Path=UserFriendlyValue}" Foreground="Green" Visibility="Hidden" Margin="16,0,0,0"/>
                </StackPanel>
              </TreeViewItem.Header>
            </TreeViewItem>

            <HierarchicalDataTemplate.Triggers>
              <!-- On change certaines propriétés dans le cas d'un NODE -->
              <DataTrigger Binding="{Binding DType}" Value="node" >
                <Setter TargetName="lbName"    Property="Foreground" Value="Black" />
                <Setter TargetName="lbName"    Property="FontWeight" Value="Bold" />
                <Setter TargetName="lbName"    Property="Width"      Value="480" />
                <Setter TargetName="lbName"    Property="Margin"     Value="2"  />
                <Setter TargetName="lbValue"   Property="Visibility" Value="Hidden" />
              </DataTrigger>
              <!-- On change certaines propriétés dans le cas d'un STRING -->
              <DataTrigger Binding="{Binding DType}" Value="REG_SZ" >
                <Setter TargetName="lbValue"   Property="Width"      Value="480" />
                <Setter TargetName="lbValue"   Property="Foreground" Value="Green" />
              </DataTrigger>
              <!-- On change certaines propriétés dans le cas d'un DWORD -->
              <DataTrigger Binding="{Binding DType}" Value="REG_DWORD" >
                <Setter TargetName="btUfUnit"  Property="Visibility" Value="Visible" />
                <Setter TargetName="lbUfValue" Property="Visibility" Value="Visible" />
              </DataTrigger>
            </HierarchicalDataTemplate.Triggers>

          </HierarchicalDataTemplate>

        </TreeView.Resources>
      </TreeView>

      <!-- ========================== -->
      <!-- BOUTON AJOUTER             -->
      <!-- ========================== -->
      <materialDesign:PopupBox Grid.Column="0" Grid.Row="3"
      Style="{StaticResource MaterialDesignMultiFloatingActionPopupBox}"
      IsEnabled="False"
      PlacementMode="LeftAndAlignMiddles"
      UnfurlOrientation="Horizontal"
      ToolTip="Manage Registry key"
      Margin="0 0 10 10"  Width="42"  Height="42"
      HorizontalAlignment="Right" VerticalAlignment="Center">
        <StackPanel Orientation="Horizontal">
          <Button ToolTip="Add a key" Command="{Binding AddCommand}">
            <Path
    Data="M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z"
    Stretch="Uniform"
    Width="14"  Height="14"
    Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" />
          </Button>
          <Button ToolTip="Remove selected key" Command="{Binding RemoveSelectedItemCommand}">
            <Path
    Data="M19,4H15.5L14.5,3H9.5L8.5,4H5V6H19M6,19A2,2 0 0,0 8,21H16A2,2 0 0,0 18,19V7H6V19Z"
    Stretch="Uniform" Width="14" Height="14"
    Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" />
          </Button>
        </StackPanel>
      </materialDesign:PopupBox>

      <!-- ========================== -->
      <!-- Barre de Recherche 1       -->
      <!-- ========================== -->
      <materialDesign:ColorZone  Grid.Column="0" Grid.Row="4" Mode="PrimaryDark" Margin="8" CornerRadius="8"
                                 IsEnabled="{Binding ElementName=TreeView1, Path=IsVisible}"
                                 materialDesign:ShadowAssist.ShadowDepth="Depth3">
        <Grid Margin="8">
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="auto"/>
          </Grid.ColumnDefinitions>
          <materialDesign:PackIcon Grid.Column="0" Kind="Search" Height="32" Width="32"/>
          <TextBlock Grid.Column="1" Margin="8" Text="Registry key:" />
          <materialDesign:ColorZone Grid.Column="2"  Mode="Light" Height="32" CornerRadius="4" materialDesign:ShadowAssist.ShadowDepth="Depth1">
            <StackPanel Orientation="Horizontal" Margin="2" >
              <materialDesign:PackIcon Kind="Magnify" VerticalAlignment="Center" />
              <TextBox Margin="8 0 12 4" materialDesign:HintAssist.Hint="Enter a key name to search..." Name="SearchedWord1"/>
            </StackPanel>
          </materialDesign:ColorZone>
          <Button Grid.Column="3"  Margin="8 0 0 0 " Click="Tree1_Search_bt">Find</Button>
        </Grid>
      </materialDesign:ColorZone>


      <!-- *************************************************************** -->
      <!--                     Separateur                                  -->
      <!-- *************************************************************** -->
      <!--<GridSplitter Grid.Column="1" Grid.RowSpan="5" Width="5" HorizontalAlignment="Stretch" />-->

      <!-- *************************************************************** -->
      <!--                     Grille de droite                            -->
      <!-- *************************************************************** -->
      <!-- ==========================-->
      <!-- Toolbar 2 -->
      <!-- ==========================-->
      <!--<ToolBarTray DockPanel.Dock="top" Grid.Column="2" Grid.Row="0">
                <ToolBar Style="{DynamicResource MaterialDesignToolBar}">
                    <Button ToolTip="Tree stats" Name="bTray2Button1" Click="bTray2Button1_Click">
                        <materialDesign:PackIcon Kind="ChartBarStacked" />
                    </Button>
                    <Button ToolTip="Length stats" Name="bTray2Button2" Click="bTray2Button2_Click">
                        <materialDesign:PackIcon Kind="ChartGantt" />
                    </Button>
                </ToolBar>
            </ToolBarTray>-->

      <!-- ==========================-->
      <!-- CHIP 2: Display Info -->
      <!-- ==========================-->
      <!--<materialDesign:Chip Grid.Column="2" Grid.Row="1" x:Name="Tree2_InfoChip"
          Content="no file loaded"
          IsDeletable="True" DeleteClick="Tree2_CloseFile_bt"  DeleteToolTip="Close the file">
                <materialDesign:Chip.Icon>
                    <materialDesign:PackIcon  Kind="File"></materialDesign:PackIcon>
                </materialDesign:Chip.Icon>
            </materialDesign:Chip>-->

      <!-- ==========================-->
      <!-- DROP ZONE 2  -->
      <!-- ==========================-->
      <!--<TextBlock x:Name="DropZone2" Grid.Column="2" Grid.Row="2" AllowDrop="True" Drop="Tree2_drop" 
                       Text="drop your registry file here" 
                       FontSize="48" Foreground="LightGray" TextAlignment="Center" TextWrapping="Wrap" VerticalAlignment="Center" Margin="40" Focusable="False" />-->
      <!-- ==========================-->
      <!-- TREE VIEW 2 avec Binding  -->
      <!-- ==========================-->
      <!--<TreeView Name="TreeView2" Grid.Column="2" Grid.Row="2"  Margin="16,16,0,0" Visibility="Hidden"
                AllowDrop="True" Drop="Tree2_drop"
                SelectedItemChanged="Tree2_SelectedItemChanged">

                -->
      <!-- On peut definir ausi le style des TreeViewItems de cette façon: -->
      <!--
                <TreeView.ItemContainerStyle>
                    <Style TargetType="TreeViewItem">
                        <Setter Property="IsSelected" Value="{Binding IsSelected, Mode=TwoWay}" />
                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                   </Style>
                </TreeView.ItemContainerStyle>

                <TreeView.Resources>
                    <HierarchicalDataTemplate
              DataType="{x:Type local:RegistryItem}"
              ItemsSource="{Binding Path=SubItem}">

                        -->
      <!-- Affichage par défaut des données -->
      <!--
                        <StackPanel Orientation="Horizontal" MinHeight="0"  Margin="0,-6,0,-6" >
                            <TextBlock x:Name="lbName"    Text="{Binding Path=Name}"  Width="220" ToolTip="{Binding Path=Name}" ToolTipService.InitialShowDelay="2000" VerticalAlignment="Center"/>
                            <TextBlock x:Name="lbValue"   Text="{Binding Path=Value}" ToolTip="{Binding Path=DType}" Width="80" VerticalAlignment="Center" />
                            <Button    x:Name="btUfUnit"  Content="{Binding Path=UserFriendlyUnit}"  Click="TreeView_ChangeUnit_Click" Style="{StaticResource OutlinedSmallButton}" Visibility="Hidden" />
                            <TextBlock x:Name="lbUfValue" Text="{Binding Path=UserFriendlyValue}" Foreground="Green" Visibility="Hidden" Margin="16,0,0,0" VerticalAlignment="Center"/>
                        </StackPanel>

                        <HierarchicalDataTemplate.Triggers>
                            -->
      <!-- On change certaines propriétés dans le cas d'un NODE -->
      <!--
                            <DataTrigger Binding="{Binding DType}" Value="node" >
                                <Setter TargetName="lbName"    Property="Foreground" Value="Black" />
                                <Setter TargetName="lbName"    Property="FontWeight" Value="Bold" />
                                <Setter TargetName="lbName"    Property="Width"      Value="480" />
                                <Setter TargetName="lbName"    Property="Margin"     Value="2" />
                                <Setter TargetName="lbValue"   Property="Visibility" Value="Hidden" />
                            </DataTrigger>
                            -->
      <!-- On change certaines propriétés dans le cas d'un STRING -->
      <!--
                            <DataTrigger Binding="{Binding DType}" Value="REG_SZ" >
                                <Setter TargetName="lbValue"   Property="Width"      Value="480" />
                                <Setter TargetName="lbValue"   Property="Foreground" Value="Green" />
                            </DataTrigger>
                            -->
      <!-- On change certaines propriétés dans le cas d'un DWORD -->
      <!--
                            <DataTrigger Binding="{Binding DType}" Value="REG_DWORD" >
                                <Setter TargetName="btUfUnit"  Property="Visibility" Value="Visible" />
                                <Setter TargetName="lbUfValue" Property="Visibility" Value="Visible" />
                            </DataTrigger>
                        </HierarchicalDataTemplate.Triggers>

                    </HierarchicalDataTemplate>
                </TreeView.Resources>
            </TreeView>-->

      <!-- ========================== -->
      <!-- BOUTON AJOUTER             -->
      <!-- ========================== -->
      <!--<materialDesign:PopupBox Grid.Column="2" Grid.Row="3"
    Style="{StaticResource MaterialDesignMultiFloatingActionPopupBox}"
    IsEnabled="False"
    PlacementMode="LeftAndAlignMiddles"
    UnfurlOrientation="Horizontal"
    ToolTip="Manage Registry key"
    Margin="0 0 10 10"  Width="42"  Height="42"
    HorizontalAlignment="Right" VerticalAlignment="Center">
        <StackPanel Orientation="Horizontal">
          <Button ToolTip="Add a key" Command="{Binding AddCommand}">
            <Path
    Data="M19,13H13V19H11V13H5V11H11V5H13V11H19V13Z"
    Stretch="Uniform"
    Width="14"  Height="14"
    Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" />
          </Button>
          <Button ToolTip="Remove selected key" Command="{Binding RemoveSelectedItemCommand}">
            <Path
    Data="M19,4H15.5L14.5,3H9.5L8.5,4H5V6H19M6,19A2,2 0 0,0 8,21H16A2,2 0 0,0 18,19V7H6V19Z"
    Stretch="Uniform" Width="14" Height="14"
    Fill="{Binding RelativeSource={RelativeSource AncestorType=Button}, Path=Foreground}" />
          </Button>
        </StackPanel>
      </materialDesign:PopupBox>-->

      <!-- ==================================================-->
      <!--           Barre de recherche 2                   -->
      <!-- ==================================================-->
      B<!--<materialDesign:ColorZone  Grid.Column="2" Grid.Row="4" Mode="PrimaryDark" Margin="8" CornerRadius="8" 
                                       IsEnabled="{Binding ElementName=TreeView2, Path=IsVisible}"
                                       materialDesign:ShadowAssist.ShadowDepth="Depth3" >
                <Grid Margin="8" >
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="auto"/>
                    </Grid.ColumnDefinitions>
                    <materialDesign:PackIcon Grid.Column="0" Kind="Search" Height="32" Width="32"/>
                    <TextBlock Grid.Column="1" Margin="8" Text="Registry key:"  />
                    <materialDesign:ColorZone Grid.Column="2"  Mode="Light" Height="32"
                                    CornerRadius="4" materialDesign:ShadowAssist.ShadowDepth="Depth1">
                        <StackPanel Orientation="Horizontal" Margin="2" >
                            <materialDesign:PackIcon Kind="Magnify" VerticalAlignment="Center" />
                            <TextBox Margin="8 0 12 4" materialDesign:HintAssist.Hint="Enter a key name to search..." Name="SearchedWord2"/>
                        </StackPanel>
                    </materialDesign:ColorZone>
                    <Button Grid.Column="3"  Margin="8 0 0 0" Click="Tree2_Search_bt" >Find</Button>
                </Grid>
            </materialDesign:ColorZone>-->


    </Grid>

    <!--==========================================================-->
    <!--                  POPUP D'INFOS SUR LE TREEVIEW           -->
    <!--==========================================================-->
    <!--  NOTES:
            PlacementTarget="{Binding ElementName=bTray2Button1}"   Placement="Bottom" 
            IsOpen="{Binding ElementName=bTray2Button1,Path=IsMouseOver}" 
            // Popup peut etre animé uniquement si le bouton ne l'est pas
            AllowsTransparency="True" PopupAnimation="Slide"  
            HorizontalAlignment="Center"    VerticalAlignment="Top" 
            -->
    <Popup
    Name="CardTreeInfo"
    Margin="10,10,0,13"
    IsOpen="False"
    Placement="Mouse"
    AllowsTransparency="True" >
      <Grid>
        <materialDesign:Card
    Margin="12"
    materialDesign:ShadowAssist.ShadowDepth="Depth3"
    UniformCornerRadius="8"
    Background="{DynamicResource PrimaryHueLightBrush}"
    Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}">
          <!--Padding="0" Width="200"-->
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" Name="logo_0"/>
              <RowDefinition Height="Auto" Name="titre_1" />
              <RowDefinition Height="Auto" Name="sepline_2" />
              <RowDefinition Height="Auto" Name="nb_nodes_3" />
              <RowDefinition Height="Auto" Name="nb_levels_4"/>
              <RowDefinition Height="Auto" Name="nb_keys_5"/>
              <RowDefinition Height="Auto" Name="sepline_and_close_6" />
            </Grid.RowDefinitions>
            <materialDesign:ColorZone Grid.Row="0"
    Mode="PrimaryDark" VerticalAlignment="Stretch">
              <materialDesign:PackIcon Kind="ChartBarStacked"
    Height="48" Width="48"
    VerticalAlignment="Center"
    HorizontalAlignment="Center" />
            </materialDesign:ColorZone>
            <TextBlock Grid.Row="1"
           Margin="18 6 18 0"
           HorizontalAlignment="Center"
           Style="{StaticResource MaterialDesignHeadlineTextBlock}"
           Foreground="{DynamicResource ResourceKey=PrimaryHueDarkBrush}"
           Text="Statistics" >
            </TextBlock>
            <Separator Grid.Row="2" Style="{StaticResource MaterialDesignLightSeparator}" />
            <StackPanel Grid.Row="3" Orientation="Horizontal" Margin="12,2,0,0">
              <TextBlock Text="Nb of nodes" />
              <TextBlock Name="tbStatNodes" Margin="12,0,0,0" />
            </StackPanel>
            <StackPanel Grid.Row="4" Orientation="Horizontal" Margin="12,2,0,0">
              <TextBlock Text="Nb of levels"/>
              <TextBlock Name="tbStatLevels" Margin="12,0,0,0" />
            </StackPanel>
            <StackPanel Grid.Row="5" Orientation="Horizontal" Margin="12,2,0,0">
              <TextBlock Text="Nb of keys"/>
              <TextBlock Name="tbStatKeys" Margin="12,0,0,0"/>
            </StackPanel>
            <Separator Grid.Row="6" Style="{StaticResource MaterialDesignLightSeparator}" />
            <Button Grid.Row="6"
        HorizontalAlignment="Right" Width="30"
        Style="{StaticResource MaterialDesignToolForegroundButton}"
        Click="CardTreeInfo_Close"
        materialDesign:RippleAssist.IsCentered="True">
              <materialDesign:PackIcon  Kind="CloseCircleOutline" />
            </Button>
          </Grid>
        </materialDesign:Card>
      </Grid>
    </Popup>


    <!--==========================================================-->
    <!--          POPUP DE STATISTIQUES SUR LE TREEVIEW           -->
    <!--==========================================================-->
    <Popup
    Name="CardlengthStats"
    Margin="10,10,0,13"
    IsOpen="False"
    Placement="Mouse"
    AllowsTransparency="True" >
      <Grid>
        <materialDesign:Card
    Margin="12"
    materialDesign:ShadowAssist.ShadowDepth="Depth3"
    UniformCornerRadius="8"
    Background="{DynamicResource PrimaryHueLightBrush}"
    Foreground="{DynamicResource PrimaryHueDarkForegroundBrush}">
          <!--Padding="0" Width="200"-->
          <Grid>
            <Grid.RowDefinitions>
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
              <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>
            <Grid.ColumnDefinitions>
              <ColumnDefinition />
              <ColumnDefinition />
              <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <materialDesign:ColorZone Grid.Row="0" Grid.ColumnSpan="3"
    Mode="PrimaryDark" VerticalAlignment="Stretch">
              <materialDesign:PackIcon Kind="ChartGantt"
    Height="48" Width="48"
    VerticalAlignment="Center"
    HorizontalAlignment="Center" />
            </materialDesign:ColorZone>
            <TextBlock Grid.Row="1" Grid.ColumnSpan="3"
           Margin="18 6 18 0"
           HorizontalAlignment="Center"
           Style="{StaticResource MaterialDesignHeadlineTextBlock}"
           Foreground="{DynamicResource ResourceKey=PrimaryHueDarkBrush}"
           Text="Length stats">
            </TextBlock>
            <Separator Grid.Row="2" Style="{StaticResource MaterialDesignLightSeparator}" />
            <TextBlock Grid.Row="3" Grid.Column="0" Text="Number of labels" Margin="12,0,0,0" />
            <TextBlock Grid.Row="3" Grid.Column="1" Text="(total)" />
            <TextBlock Grid.Row="3" Grid.Column="2" Name="nbItems" Margin="12,0,0,0"/>
            <TextBlock Grid.Row="4" Grid.Column="0" Text="Average length" Margin="12,0,0,0" />
            <TextBlock Grid.Row="4" Grid.Column="1" Name="tbAvLength" Margin="12,0,0,0" />
            <TextBlock Grid.Row="4" Grid.Column="2" Name="nbAvLength" Margin="12,0,12,0" />
            <TextBlock Grid.Row="5" Grid.Column="0" Text="Modal length"  Margin="12,2,0,0"/>
            <TextBlock Grid.Row="5" Grid.Column="1" Name="tbModelength" Margin="12,0,0,0" />
            <TextBlock Grid.Row="5" Grid.Column="2" Name="nbModelength" Margin="12,0,0,0" />
            <TextBlock Grid.Row="6" Grid.Column="0" Text="Standard Deviation" Margin="12,0,0,0"/>
            <TextBlock Grid.Row="6" Grid.Column="1" Name="tbSD" Margin="12,0,0,0"/>
            <TextBlock Grid.Row="7" Grid.Column="0" Text="Inf Av+SD (84%)" Margin="12,0,0,0"/>
            <TextBlock Grid.Row="7" Grid.Column="1" Name="tbSD84" Margin="12,0,0,0"/>
            <TextBlock Grid.Row="7" Grid.Column="2" Name="nbSD84" Margin="12,0,0,0"/>
            <TextBlock Grid.Row="8" Grid.Column="0" Text="Inf Av+2*SD (98%)" Margin="12,0,0,0"/>
            <TextBlock Grid.Row="8" Grid.Column="1" Name="tbSD98" Margin="12,0,0,0"/>
            <TextBlock Grid.Row="8" Grid.Column="2" Name="nbSD98" Margin="12,0,0,0"/>
            <Button Grid.Row="9"  Grid.Column="2"
        HorizontalAlignment="Right" Width="30"
        Style="{StaticResource MaterialDesignToolForegroundButton}"
        Click="CardlengthStats_Close"
        materialDesign:RippleAssist.IsCentered="True">
              <materialDesign:PackIcon  Kind="CloseCircleOutline" />
            </Button>
          </Grid>
        </materialDesign:Card>
      </Grid>
    </Popup>

    <!--==========================================================-->
    <!--           ESSAIS DIVERS                                  -->
    <!--==========================================================-->
    <!--<materialDesign:PopupBox 
    Name="PopTreeInfo"
    PlacementMode="BottomAndAlignLeftEdges"
    Margin="10,10,0,13" Width="200" Height="400"
    HorizontalAlignment="Left"
    VerticalAlignment="Top" 
    materialDesign:ShadowAssist.ShadowDepth="Depth5"
    >
            -->
    <!--
    IsOpen="False"
    PlacementTarget="{Binding ElementName=bTray2Button1}"   Placement="Bottom" 
        IsOpen="{Binding ElementName=bTray2Button1,Path=IsMouseOver}" 
        // Popup peut etre animé uniquement si le bouton ne l'est pas
        AllowsTransparency="True" PopupAnimation="Slide"  
        Placement="Mouse" 
        PlacementTarget="{Binding ElementName=bTray2button1}"   Placement="Bottom" 
    -->
    <!--mc:Ignorable="d" d:DesignHeight="200" d:DesignWidth="200"-->
    <!--

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>
                <materialDesign:ColorZone Grid.Row="0" Mode="PrimaryDark" VerticalAlignment="Stretch">
                    <materialDesign:PackIcon Kind="ChartBarStacked" 
                        Height="60" Width="60"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Center" />
                </materialDesign:ColorZone>
                <TextBlock 
                        Grid.Row="1" 
                        Margin="16 16 16 4" 
                        Style="{StaticResource MaterialDesignHeadlineTextBlock}"
                        Foreground="{DynamicResource ResourceKey=PrimaryHueDarkBrush}">
                Tree statistics:
                </TextBlock>
                <Separator Grid.Row="2" Style="{StaticResource MaterialDesignLightSeparator}" />

                <StackPanel Grid.Row="3" Orientation="Vertical">
                    <TextBlock Text="Nb of Nodes"  Style="{StaticResource MaterialDesignBody2TextBlock}"></TextBlock>
                    <TextBlock Text="Nb of keys"   Style="{StaticResource MaterialDesignBody2TextBlock}"></TextBlock>
                    <TextBlock Text="Nb of levels" Style="{StaticResource MaterialDesignBody2TextBlock}"></TextBlock>
                </StackPanel>

                -->
    <!--<Button Grid.Row="4" HorizontalAlignment="Right"
        Style="{StaticResource MaterialDesignToolForegroundButton}"
        Width="30"
        Padding="2 0 2 0"
        materialDesign:RippleAssist.IsCentered="True">
                    <materialDesign:PackIcon
          Kind="Phone" />
                </Button>-->
    <!--
                <materialDesign:PopupBox Grid.Row=" 4"
                        Style="{StaticResource MaterialDesignToolForegroundPopupBox}"
        HorizontalAlignment="Right"
        Padding="2 0 2 0">
                    -->
    <!--<StackPanel Orientation="Horizontal">
                        <Button Content="More" />
                        <Button Content="Options" />
                    </StackPanel>-->
    <!--
                </materialDesign:PopupBox>
            </Grid>
        </materialDesign:PopupBox>-->

    <!--<StackPanel Grid.Row="7" Orientation="Horizontal" Margin="12,2,0,0" Visibility="Hidden">
      <TextBlock Text="Col width" />
      <TextBlock Text="{Binding ElementName=TreeView2, Path=ActualWidth}" Margin="12,0,0,0" />
    </StackPanel>
    <StackPanel Grid.Row="8" Orientation="Horizontal" Margin="12,2,0,0" Visibility="Hidden">
      <TextBlock Text="parent width" />
      <TextBlock Text="{Binding ElementName=dockpanel, Path=ActualWidth}" Margin="12,0,0,0" />
    </StackPanel>-->



  </DockPanel>

</Window>
